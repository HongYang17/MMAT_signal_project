# === Core ML & Data Stack ===
numpy>=1.22
pandas>=1.4
scikit-learn>=1.0
xgboost>=1.6
joblib>=1.1

# === Visualization ===
matplotlib>=3.5
seaborn>=0.11
plotly>=5.6

# === Binance API & Environment ===
python-binance>=1.0
python-dotenv>=0.20

# === Utility ===
tqdm

# === TA-Lib (Optional — must install manually) ===
# Windows: download from https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib
# macOS:   brew install ta-lib && pip install TA-Lib
# Linux:   sudo apt install libta-lib0 libta-lib-dev && pip install TA-Lib
#
# Once installed manually, uncomment below:
# TA-Lib>=0.4.0
